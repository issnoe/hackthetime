import React, { useState } from "react";
import "./component.css";

export type NameComponentProps = {
  name: string;
};

const NameComponent = ({ name }: NameComponentProps) => {
  return <span></span>;
};

export default NameComponent;
